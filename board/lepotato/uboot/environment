kernel_addr_r=0x21000000
fdt_addr_r=0x20000000

bootargs=root=/dev/mmcblk0p2 rootwait
bootcmd=fatload mmc 0:1 ${kernel_addr_r} Image; fatload mmc 0:1 ${fdt_addr_r} system.dtb; printenv bootargs; booti ${kernel_addr_r} - ${fdt_addr_r};
