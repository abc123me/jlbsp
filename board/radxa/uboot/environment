kernel_addr_r=0x11000000
fdt_addr_r=0x10000000
bootcmd=fatload mmc 1:1 ${kernel_addr_r} Image; fatload mmc 1:1 ${fdt_addr_r} rk3566-radxa-zero-3e.dtb; setenv bootargs root=/dev/mmcblk0p2 rootwait; echo booting from MMC1; printenv bootargs; booti ${kernel_addr_r} - ${fdt_addr_r};
